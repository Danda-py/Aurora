---
alwaysApply: true
---

Ensure all user-facing text strings are fully dynamic and support all five configured languages ('it', 'en', 'de', 'fr', 'es'). Retrieve translations exclusively from the global translations map (e.g. `VIDEO_TRANSLATIONS` or custom translations context) and never hardcode UI text.